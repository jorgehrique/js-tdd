var expect = require('chai').expect;
/*
describe.skip('Main', function () {
  var numbers;

  before('Inicio Main', function () {
    console.log("Iniciando Testes ...")
  })

  beforeEach(function () {
    numbers = [1, 2, 3];
  })

  it('should have size 4 if add a number', function () {
    numbers.push(4);
    expect(numbers).to.have.lengthOf(4);
  })

  it('should have size 2 if remove a number', function () {
    numbers.pop();
    expect(numbers).to.have.lengthOf(2);
  })

  it('should not contains number 3 if remove a number', function () {
    numbers.pop();
    expect(numbers).to.not.contains(3);
  })
});*/